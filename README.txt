To run the command-line application:
1. in command prompt navigate to the app folder where there is pom.xml file, e.g. cd Downloads\ConsoleApp
2. type mvn compile to compile the project
3. type mvn exec:java -Dexec.mainClass=app.Main to execute the project